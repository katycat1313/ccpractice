import React from 'react';
import ReactDOM from 'react-dom/client';
import ColdCallingCoachApp from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ColdCallingCoachApp />
    </React.StrictMode>
);
